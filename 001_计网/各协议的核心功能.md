**解析**：各协议的核心功能：
- DNS（域名系统）：负责将域名（如www.njupt.edu.cn）解析为对应的IP地址，是主机获取未知域名IP的关键协议（选项A正确）；
- FTP（文件传输协议）：用于在客户端和服务器之间传输文件（B错误）；
- ICMP（互联网控制消息协议）：用于传递网络差错报告、控制信息（如ping命令基于ICMP）（C错误）；
- SMTP（简单邮件传输协议）：用于发送电子邮件（D错误）。